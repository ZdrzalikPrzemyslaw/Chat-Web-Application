//import Vue from 'vue';
import Vuex from 'vuex';

import {auth} from './auth.module';

//Vue.use(Vuex);

export default Vuex.createStore({
    state: {},
    mutations: {},
    actions: {},
    modules: { auth }
});