openapi: 3.0.2
info:
  version: 1.0.0
  title: https://czatmat.azurewebsites.net/api
  description: >

    This is ChatMat API documentation.
  
tags:
  - name: Registration
    description: Example registration operation.
  - name: User
    description: Operations about user
servers:
  - url: "https://czatmat.azurewebsites.net/api/registration"
  - url: "https://example.com/api/v1"
paths:
  /registration:
    $ref: paths/registration.yaml
  /login:
    $ref: paths/login.yaml
  /search:
    $ref: paths/searchUsers.yaml
components:
  securitySchemes:
    token:
      $ref: components/securitySchemes/token.yaml
